<div class="container" style="color: #006DAA">
  <div class="row justify-content-center">
    <h1 style="margin-bottom: 1em">My Sock</h1>
      <%= render 'form' %>
      <%= link_to 'Back', sock_path(@sock), class: "back-btn btn btn-primary my-5" %>
  </div>
</div>
